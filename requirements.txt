# The frontend runs one stack for every implementation it can load, so these
# pins follow the current course exercises (Exercise 3, verified 2026-09-18)
# rather than the 2024 versions this demo started with.
langgraph==1.2.11          # the process framework: state, nodes, edges, compile
langchain-core==1.6.2      # message types (AIMessage) used in the state
pydantic==2.13.5           # the schema check at the contract boundary
requests==2.34.2           # the Pizza API client
python-dotenv==1.2.3       # read .env, so no key ever lands in the repository
openai==3.13.0             # the OpenAI-compatible endpoint (ILaaS, HTWK GPU)
SPARQLWrapper==2.0.0       # Wikidata, through the Qanary pipeline
fuzzywuzzy                 # the fuzzy menu match of the checker
python-Levenshtein         # makes fuzzywuzzy fast (and silences its warning)
streamlit>=1.42            # the web frontend (keyed containers -> element ids)
langsmith                  # optional: tracing and the evaluation in test_pizzabot.py
